model User {
  id   Int    @id @default(autoincrement())
  name String

  wallet Wallet[]
}

model Wallet {
  id     Int @id @default(autoincrement())
  userId Int

  user User @relation(fields: [userId], references: [id])
}
